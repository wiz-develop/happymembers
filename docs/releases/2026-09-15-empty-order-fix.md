# 空注文・重複注文の再発防止

## 本番反映判定

テスト環境でE2E検証済み。本番反映対象は次の3ファイルだけとする。

- `functions.php`
- `page-cart__confirmation.php`
- `assets/js/order.js`

`test` ブランチ全体やテーマ全体を本番FTPへ同期してはならない。テスト専用ファイル、API設定、認証情報、メール送信先、デバッグ処理は本番へ移さない。

## 検証結果

- 正常注文：HTTP 200 / `code=0`
- 正常注文後の空カート送信：HTTP 409 / `code=2`
- 同一注文トークンの再送：HTTP 409 / `code=3`
- 未ログイン注文：HTTP 401 / `code=4`
- 空注文および重複注文は作成されなかった

## 本番反映手順

1. 本番FTPから対象3ファイルを再取得し、Gitの本番基準との差分を確認する。
2. 本番現行の対象3ファイルをローカルへ退避する。
3. テスト環境固有差分を含まない、本番基準の修正版3ファイルだけをアップロードする。
4. アップロード後のハッシュをローカル修正版と照合する。
5. ログイン画面の表示と、未ログイン注文APIがHTTP 401で拒否されることを確認する。

## 本番反映記録

2026-09-15に本番へ反映した。

- 本番コードコミット：`4ad2ed1`
- FTP反映：上記3ファイルのみ
- アップロード直前：本番3ファイルが初回取得時の `main` と一致
- アップロード後：本番3ファイルが本番修正版と一致
- ログイン画面：HTTP 200、ブラウザのJavaScriptエラーなし
- 修正版 `order.js`：HTTP 200、二重送信防止コードを確認
- 未ログイン注文API：HTTP 401 / `code=4`

この反映作業では、本番専用ファイル、企業サイト、環境設定ファイルに変更を加えていない。実注文を伴う後続のE2E検証は次節に記録する。

## 本番E2E検証記録

本番でのテスト注文1件およびメール送信について許可を得たうえで、2026-09-15にAndroid Pixel 7相当のE2Eを実行した。

- 有効な本番テスト注文：1件だけ受付（HTTP 200 / `code=0`）
- 作成されたテスト注文：`WB26000380`（2026-09-15 15:22:09）
- 別の注文確認画面から、受付後の空カートを送信：拒否（HTTP 409 / `code=2`）
- 受付済みの同一注文トークンを再送：拒否（HTTP 409 / `code=3`）
- 実行後の専用テスト会員カート：0件
- 注文履歴画面：正常表示、ブラウザのJavaScriptエラーなし

本番で作成された注文は1件だけで、空注文と重複注文は作成されなかった。本番テスト注文の削除・取消は実施していない。
